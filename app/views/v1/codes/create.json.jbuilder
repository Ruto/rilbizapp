json.data do
  json.code do
    json.call(
      @code,
      :id,
      :alias,
      :cost_code,
      :barcode,
      :system_code,
      :product_id,
      :codable_id,
      :codable_type,
      :active,
      :user_id,
      :created_at,
      :updated_at

    )
  end
end
